apiVersion: v1
kind: Pod
metadata:
  name: worker-app-pod
  labels:
    name: workerApp-pod
    app: votingApp
spec:
  containers:
  - name: worker-app
    image: kodekloud/examplevotingapp_worker:v1