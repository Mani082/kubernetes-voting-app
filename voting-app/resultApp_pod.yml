apiVersion: v1
kind: Pod
metadata:
  name: result-app-pod
  labels:
    name: resultApp-pod
    app: votingApp
spec:
  containers:
  - name: result-app
    image: kodekloud/examplevotingapp_result:v1
    ports:
      - containerPort: 80
